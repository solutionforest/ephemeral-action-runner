# Image Package

Provider-neutral image build abstractions can move here if the image build surface grows beyond the current manager-owned flow. Tart and WSL already have provider-specific build steps, while shared Ubuntu guest provisioning and image install scripts remain coordinated from the pool manager.
